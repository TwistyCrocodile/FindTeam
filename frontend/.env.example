# Leave empty to use Vite dev proxy (/api -> http://localhost:8080).
# Or set full backend origin if you open the app from another host/port:
# VITE_API_BASE_URL=http://localhost:8080

VITE_API_BASE_URL=
